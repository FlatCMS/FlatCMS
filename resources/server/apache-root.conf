# FlatCMS Apache template for a project-root document root.
# Replace /path/to/flatcms only. Private trees remain blocked by .htaccess.
DocumentRoot "/path/to/flatcms"

<Directory "/path/to/flatcms">
    Options +FollowSymLinks -Indexes
    AllowOverride All
    Require all granted
</Directory>
